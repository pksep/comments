# Comments

Микросервис для комментарии.

## 🚀 Быстрый старт

### Требования
- Go 1.21+
- PostgreSQL 14+
- Make
- Air (для hot-reload в режиме разработки)
  ```bash
  # Установка Air
  go install github.com/air-verse/air@latest
  
  # Проверка установки
  air -v
  ```

### Установка

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/pksep/comments.git
   cd comments
   ```

2. Настройте переменные окружения:
   ```bash
   cp .example.env .env
   # Отредактируйте .env под ваши настройки
   ```

3. Запустите базу данных:
   ```bash
   docker-compose up -d postgres
   ```

4. Установите зависимости:
   ```bash
   go mod download
   ```

5. Запустите сервер:
   ```bash
   make run-dev
   ```

## 🛠️ Доступные команды

- `make run-dev` - запуск сервера в режиме разработки
- `make run-prod` - запуск сервера в режиме продакшена
- `make swag` - генерация Swagger документации
- `make dev` - генерация Swagger + запуск сервера
- `make test` - запуск тестов
- `make lint` - проверка кода с помощью golangci-lint
- `make migrate-create` - создать новую миграцию (NAME=description)
- `make help` - показать все доступные команды

## 📚 Документация

Документация проекта включает:
- [Архитектурные решения (ADR)](/docs/ADR/)
- [Руководство по миграциям](/docs/MIGRATIONS.md)
- [Шаблон создания модуля](/docs/MODULE_TEMPLATE.md)

### Работа с диаграммами Mermaid

Для удобной работы с диаграммами Mermaid в документации рекомендуется установить следующие расширения для VS Code:

1. **Mermaid Preview** - предпросмотр диаграмм в реальном времени
    - ID: `vstirbu.vscode-mermaid-preview`
    - Позволяет просматривать диаграммы Mermaid прямо в редакторе

2. **Mermaid Editor** - визуальный редактор диаграмм
    - ID: `bradlc.vscode-tailwindcss`
    - Предоставляет удобный интерфейс для создания и редактирования диаграмм

После установки расширений:
1. Откройте файл с расширением `.md`
2. Нажмите `Ctrl+Shift+P` (или `Cmd+Shift+P` на Mac)
3. Выберите "Mermaid: Open Preview" для предпросмотра диаграммы

Диаграммы Mermaid автоматически рендерятся в GitHub и GitLab.

После запуска сервера документация API будет доступна по адресу:
- Swagger UI: http://localhost:5001/swagger/index.html
- JSON: http://localhost:5001/swagger/doc.json

## 🏗️ Структура проекта

```
.
├── cmd/                 # Точка входа приложения
│   └── server/          
│       └── main.go     
├── configs/            # Конфигурационные файлы
├── docs/               # Документация
├── internal/           # Внутренние пакеты
│   ├── api/            # API хендлеры и роуты
│   ├── app/            # Инициализация приложения
│   ├── config/         # Конфигурация
│   ├── db/             # Работа с базой данных
│   └── modules/        # Бизнес-логика приложения
│       └── comments/      # Модуль комментарии
│           ├── api/    # API хендлеры
│           ├── model/  # Модели данных
│           └── repository/ # Работа с БД
├── migrations/         # Миграции базы данных
└── pkg/               # Вспомогательные пакеты

## Migrations

### Install migrate tools
#### MacOS
brew install golang-migrate

#### Linux (смотреть актуальный релиз на GitHub)
curl -L https://github.com/golang-migrate/migrate/releases/download/v4.21.3/migrate.linux-amd64.tar.gz | tar xvz
sudo mv migrate.linux-amd64 /usr/local/bin/migrate

## 🧪 Тестирование

Для запуска тестов выполните:
```bash
make test
```

## 📄 Лицензия

Распространяется под лицензией MIT. См. `LICENSE` для получения дополнительной информации.